1)

index.html
<li><a href='javascript:Load(PUTPAGENAME)Page()'>Page</a>


2)

PageControl.js
/**
 * Description: Load the Page for signing up, into the main Div
 * return: The HTML content for the sign-up page
 */
function Load(PUTPAGENAME)Page(){
	var xmlhttp;
	if (window.XMLHttpRequest) {// code for IE7+, Firefox, Chrome, Opera, Safari
		xmlhttp=new XMLHttpRequest();
	}
	else {// code for IE6, IE5
		xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");
	}
	xmlhttp.onreadystatechange = 
		function(){
			if (xmlhttp.readyState==4 && xmlhttp.status==200){
				document.getElementById("MainContent").innerHTML=xmlhttp.responseText;
		    }
		}
	xmlhttp.open("GET","view/pages/PAGENAME.html",true);
	xmlhttp.send();
	}
	
3)

Create an html from your PAGENAME and overlay some interesting content.
Pictures anything, just fill space and make it look nice.
